#include"../файлы заголовков/scence/scence.h"
/*
void Scence::add_obj(bool standart_comps = true)
{

	if (standart_comps) {
		objects.push_back(Entity());
		size++;
		//objects.back().add_component(Type::drawComponent);
		//objects.back().add_component(Type::shapeComponent);
		

		objects.back().add_component<DrawComponent>();
	}
	else {
		objects.push_back(Entity());
		size++;
	}
}

void Scence::add_obj_comp(int objname,Type comp)
{	
	//get_obj(objname)add_component(comp);
}
*/